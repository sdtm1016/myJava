单例模式Singleton
单一实例
	应用常会：有些对象只需要一个就足够了,如回收站
	作用：保证整个应用程序中某一个实例有且只有一个
	类型：饿汉模式，懒汉模式

// 单例模式,降低耦合度(解耦合)
Runtime rt = Runtime.getRuntime();

静态方法与单例模式:
单例模式的对象都可以使用静态来实现,那么什么时候用单例,什么时候用静态
	静态一般情况下,是一个独立的非常简单的个体
	
		